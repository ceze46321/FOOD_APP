name: Build Android AAB

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4  # Updated to v4 for better compatibility

    - name: Set up JDK 11
      uses: actions/setup-java@v4  # Updated to v4 for better compatibility
      with:
        java-version: '11'

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.16.0' # Match your current version
        channel: 'stable'

    - name: Install dependencies
      run: flutter pub get

    - name: Build AAB
      run: flutter build appbundle --release --verbose

    - name: Verify AAB exists
      run: ls -la build/app/outputs/bundle/release/

    - name: Upload AAB
      uses: actions/upload-artifact@v4  # Updated to v4 to fix the error
      with:
        name: appbundle
        path: build/app/outputs/bundle/release/app-release.aab
